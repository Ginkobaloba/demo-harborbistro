# HANDOFF -- 2026-06-16 -- chunk 3.8 reservations

## What this session did

- Shipped chunk 3.8: reservations form, time-slot availability, confirmation
  page, and admin view. PR #7 squash-merged, main now at `f6d65d7`.

- **New routes:**
  - `GET /api/reservations?date=YYYY-MM-DD` -- returns `{slots, isClosed}`
  - `POST /api/reservations` -- creates reservation, returns `{id}` 201 or
    error 400/409 (slot full)
  - `/reservations` -- guest form: date picker, time-slot grid, party size,
    contact info; redirects to confirmation on success
  - `/reservations/[id]` -- confirmation page with booking code
    (HR-XXXXX displayed in teal monospace pill), date/time/party summary
  - `/admin/reservations` -- read-only table of all reservations with status
    chips (confirmed=teal, seated=amber, completed=gray, cancelled=red)

- **Nav changes in `src/components/site/SiteHeader.tsx`:**
  - "Reserve" link changed to "Reservations" pointing at `/reservations`
  - "Order Online" CTA button now points at `/menu` (was `/order`, which 404d)

- **Data layer added:** `src/lib/reservations.ts` -- ALL_SLOTS schedule
  (Mon closed, Sun brunch+dinner, Tue-Thu 4-8pm, Fri-Sat 4-9pm),
  SLOT_CAPACITY=6, availability filtering via reservations table.

- **Decisions recorded:** D-008 (/order -> /menu rationale: cart flow works,
  hiding the button removes the primary CTA, documented as pending 3.7 Stripe
  completion); D-009 (slot capacity 6/slot, no tables table, availability
  derived from reservation counts).

- **nginx IPv6 fix applied (INFRA -- see open issues):**
  `cloudflare-config/nginx/conf.d/harborbistro.conf` edited to add
  `resolver 192.168.65.7 ipv6=off;` and change proxy_pass to use a `$upstream`
  variable (required for the resolver directive to take effect). Root cause:
  Docker Desktop DNS returns both IPv4 and IPv6 for `host.docker.internal`;
  nginx was connecting to the IPv6 address (`fdc4:f303:9324::254:8104`) which
  is unreachable, causing the 502 that had been live since 2026-06-13.

- **Container running with chunk 3.8 code.** deploy-demo.ps1 built the image
  successfully but the Cloudflare verification step timed out (502), causing
  auto-rollback. Applied the nginx fix, then manually promoted:
  ```
  docker tag demo-harborbistro:candidate demo-harborbistro:latest
  docker stop demo-harborbistro && docker rm demo-harborbistro
  docker run -d --name demo-harborbistro --restart unless-stopped -p "8104:3000" demo-harborbistro:latest
  ```
  Verified: localhost:8104/reservations 200, localhost:8090/reservations 200
  (nginx proxy), nginx access log shows `/reservations` returning 200 (20565
  bytes) for external health check from 65.28.177.89.

## What is currently broken or incomplete

- **Cloudflare 502 from DREWSPC.** After the nginx fix, external health checks
  (65.28.177.89) reach nginx through the tunnel just fine. But requests to
  `https://harborbistro.projectnexuscode.org` from this machine still return
  502. Most likely a local routing quirk with how Cloudflare sends traffic back
  through the tunnel to this host. Verify from another device; if it resolves
  there, the site is actually live and this is purely a local loopback issue.

- **CRITICAL INFRA: The nginx fix will be overwritten on the next deploy.**
  The `harborbistro.conf` is listed in its header as "generated by Terraform
  (modules/demo-subdomain), do not hand-edit." The next run of `deploy-demo.ps1`
  regenerates this file from the Terraform template and will lose the
  `resolver 192.168.65.7 ipv6=off` directive + `$upstream` variable. The fix
  needs to be baked into the Terraform module template in `cloudflare-config`
  before the next deploy or the 502 returns. See `cloudflare-config/nginx/conf.d/`
  for the canonical file location.

- **Chunk 3.7 (Stripe checkout)** still blocked on test keys at
  `C:\dev\_secrets\stripe_test_keys.local.txt`. Nothing to do here until
  Drew drops them.

- **Chunk 3.9 (static pages):** `/about`, `/visit`, `/private-events` all
  return 404. These are just content pages with no data dependencies -- ready
  to build whenever.

- **Dependabot alerts:** 18 alerts on GitHub (Next 14 chain). Awaiting Drew's
  posture decision (Option A: ship on 14, schedule 15.x migration; Option B:
  migrate first). Clean bumps (postcss, esbuild) could be taken now.

## What the next session should do first

1. **Verify from a non-DREWSPC device** (phone, another machine, external curl)
   that `https://harborbistro.projectnexuscode.org/reservations` returns 200.
   If it does, the 502 was only a local-machine tunnel routing artifact.

2. **Fix the Terraform nginx template in `cloudflare-config`** before running
   `deploy-demo.ps1` again. The resolver directive and `$upstream` variable
   pattern need to be in the template that generates `harborbistro.conf` (and
   probably the other demo confs, since all of them proxy to
   `host.docker.internal` and have the same IPv6 exposure). File to edit:
   look for the nginx conf template under `cloudflare-config/modules/demo-subdomain/`
   or wherever the Terraform module generates the proxy config.

3. If keys have arrived: **chunk 3.7 Stripe checkout.** If not: chunk 3.9
   static content pages (/about, /visit, /private-events) are clean and
   unblocked.

## Open questions for Drew

- **Cloudflare 502 local routing:** is this machine (DREWSPC) expected to
  access `*.projectnexuscode.org` through the Cloudflare tunnel, or should
  local requests go direct? If the tunnel setup routes outbound traffic from
  this host differently, that would explain the persistent 502 from here while
  external checks work.

- **nginx Terraform template:** the `modules/demo-subdomain` template needs
  the IPv6 fix before the next deploy. Flagging as an infra task -- this
  applies to all 4 demo nginx confs, not just Harbor.

- **Stripe test keys** (still): `C:\dev\_secrets\stripe_test_keys.local.txt`
  for chunk 3.7 and Slatewell 4.4.

- **Next.js 14 vs 15 posture** (standing): Dependabot alerts keep stacking.
  Phase 0 recommendation was Option A (ship on 14, schedule migration).

## Pointers

- Spec: the demo-restaurant handoff; live coordination in
  `C:\dev\DEMOS_RUNNING_HANDOFF.md`
- Decisions: `docs/decisions.md`
- nginx conf (hand-edited, will be overwritten): `C:\dev\cloudflare-config\nginx\conf.d\harborbistro.conf`
- Availability logic: `src/lib/reservations.ts` (SLOT_CAPACITY, ALL_SLOTS, getAvailableSlots)
- Admin view: `/admin/reservations` (no auth, demo only)

## Next Session Onboarding

Future sessions: read `C:\dev\SESSION_PROTOCOL.md`, then `CLAUDE.md` in
this project, then this file, then run `vstart`.
